{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug SFML App",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/Build/${fileBasenameNoExtension}",
            "args": [],
            "preLaunchTask": "build",
            "cwd": "${workspaceFolder}",
            "initCommands": [
                "settings set target.env-vars DYLD_LIBRARY_PATH=/opt/homebrew/lib"
            ],
            "internalConsoleOptions": "openOnSessionStart"
        }
    ]
}
